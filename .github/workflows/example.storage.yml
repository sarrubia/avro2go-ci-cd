name: example.storage
on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/avro/schm/example/storage/**'
  push:
    branches:
      - main
    paths:
      - 'src/avro/schm/example/storage/**'

permissions:
  contents: write
  pull-requests: write

env:
  S_API_KEY: ${{ secrets.SPLIT_API_KEY }}

jobs:
  ci-cd:
    uses: ./.github/workflows/subdomain.ci-cd.yml
    with:
      domain: 'example'
      subdomain: 'storage'
      subdomainPath: 'src/avro/schm/example/storage'
      splitApiKey: ${{env.S_API_KEY}}